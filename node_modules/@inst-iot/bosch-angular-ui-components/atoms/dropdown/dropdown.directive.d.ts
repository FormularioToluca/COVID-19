import { ComponentFactoryResolver, ComponentRef, OnDestroy, OnInit, TemplateRef, ViewContainerRef } from '@angular/core';
import { DropdownComponent } from './dropdown.component';
export declare class DropdownDirective implements OnDestroy, OnInit {
    private factoryResolver;
    private viewContainerRef;
    position: 'left' | 'right' | 'center';
    autoClose: boolean;
    renderOnInit: boolean;
    hugContent: boolean;
    private _context;
    openOnClick: boolean;
    tpl: TemplateRef<any>;
    componentRef: ComponentRef<DropdownComponent>;
    closeSub: any;
    constructor(factoryResolver: ComponentFactoryResolver, viewContainerRef: ViewContainerRef);
    ngOnDestroy(): void;
    ngOnInit(): void;
    template: TemplateRef<any>;
    onClick(e: MouseEvent): void;
    context: Record<string, any>;
    renderDropdown(): void;
    openDropdown(): void;
    closeDropdown(): void;
}
