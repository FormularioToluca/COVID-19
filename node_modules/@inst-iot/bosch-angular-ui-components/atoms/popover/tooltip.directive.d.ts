import { ComponentFactoryResolver, ComponentRef, ElementRef, OnDestroy, TemplateRef, ViewContainerRef } from '@angular/core';
import { PopoverComponent } from './popover.component';
export declare class TooltipDirective implements OnDestroy {
    private factoryResolver;
    private viewContainerRef;
    position: 'bottom' | 'top' | 'left' | 'right';
    /**
     * Disables that the tooltip stays open when clicked.
     * Instead the tooltip is closed.
     */
    noFix: boolean;
    context: Record<string, any>;
    anchor: ElementRef | HTMLElement;
    content: TemplateRef<any> | string;
    componentRef: ComponentRef<PopoverComponent>;
    private closeSub;
    private fixed;
    constructor(factoryResolver: ComponentFactoryResolver, viewContainerRef: ViewContainerRef);
    ngOnDestroy(): void;
    template: TemplateRef<any> | string;
    onClick(e: MouseEvent): void;
    onEnter(e: MouseEvent): void;
    onLeave(e: MouseEvent): void;
    openTooltip(): void;
    updateComponentState(): void;
    closeTooltip(): void;
}
