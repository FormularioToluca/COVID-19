import { OnChanges, SimpleChanges } from '@angular/core';
export declare class CalloutComponent implements OnChanges {
    type: 'error' | 'warning' | 'info' | 'success';
    icon: string[];
    type2icon: {
        error: string;
        success: string;
        warning: string;
        info: string;
    };
    constructor();
    ngOnChanges(changes: SimpleChanges): void;
}
