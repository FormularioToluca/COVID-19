import { ApplicationRef, ComponentFactoryResolver, Injector, TemplateRef } from '@angular/core';
import { ModalService } from './modal.service';
export declare class ModalDirective {
    private modalService;
    content: string | TemplateRef<any>;
    showClose: boolean;
    position: 'middle' | 'top' | 'bottom';
    maxWidth: string;
    backdropClose: boolean;
    stacked: boolean;
    constructor(modalService: ModalService, factoryResolver: ComponentFactoryResolver, injector: Injector, appRef: ApplicationRef);
    click(event: MouseEvent): void;
    rbModal: string | TemplateRef<any>;
}
