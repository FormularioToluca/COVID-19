import { AfterViewInit, ComponentRef, ElementRef, OnInit, TemplateRef, Type, ViewContainerRef } from '@angular/core';
export interface ModalOptions {
    position?: 'middle' | 'top' | 'bottom';
    showClose?: boolean;
    maxWidth?: string;
    stacked?: boolean;
    backdropClose?: boolean;
}
export declare class ModalComponent implements OnInit, AfterViewInit {
    private elementRef;
    private viewContainerRef;
    contentText: string;
    contentTpl: TemplateRef<any>;
    contentComponent: Type<any>;
    contentComponentRef: ComponentRef<any>;
    options: ModalOptions;
    close: (reason?: any) => void;
    componentInstance: any;
    box: ElementRef;
    constructor(elementRef: ElementRef, viewContainerRef: ViewContainerRef);
    ngOnInit(): void;
    ngAfterViewInit(): void;
}
