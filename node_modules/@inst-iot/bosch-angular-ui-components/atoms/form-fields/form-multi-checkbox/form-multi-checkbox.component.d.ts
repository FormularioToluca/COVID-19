import { ElementRef, OnChanges, OnInit, QueryList, Renderer2, SimpleChanges, TemplateRef } from '@angular/core';
import { ControlValueAccessor } from '@angular/forms';
import { FormValidationMessageDirective } from '../form-validation-message.directive';
import { emptyFunction } from '../forms-util';
export declare class FormMultiCheckboxComponent implements OnInit, OnChanges, ControlValueAccessor {
    private renderer;
    id: string;
    name: string;
    label: string | TemplateRef<any>;
    state: any;
    numStates: number;
    input: ElementRef;
    messages: QueryList<FormValidationMessageDirective>;
    stateLabels: string[];
    statesValues: boolean[];
    stateLabel: string;
    labelElement: ElementRef;
    onChange: typeof emptyFunction;
    onTouched: typeof emptyFunction;
    constructor(renderer: Renderer2);
    ngOnInit(): void;
    isLabelTemplate(): boolean;
    ngOnChanges(changes: SimpleChanges): void;
    setState(stateValue: any): void;
    toggleState(): void;
    writeValue(value: any): void;
    registerOnChange(fn: (_: any) => {}): void;
    registerOnTouched(fn: () => {}): void;
    setDisabledState(isDisabled: boolean): void;
}
