import { CustomValueConverter } from '../form-input/form-input.component';
export declare class NumberConverterDirective implements CustomValueConverter {
    modelToValue(model: number | null): string;
    valueToModel(value: string): number;
}
