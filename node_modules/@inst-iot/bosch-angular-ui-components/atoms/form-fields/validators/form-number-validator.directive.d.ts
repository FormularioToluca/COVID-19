import { AbstractControl, ValidationErrors, Validator } from '@angular/forms';
export declare class FormNumberValidatorDirective implements Validator {
    rbMin: number;
    rbMax: number;
    constructor();
    validate(control: AbstractControl): ValidationErrors | null;
}
