import { AbstractControl, ValidationErrors, Validator } from '@angular/forms';
export declare class FormChipsInputPatternValidatorDirective implements Validator {
    pattern: any;
    constructor();
    validate(c: AbstractControl): ValidationErrors | null;
}
