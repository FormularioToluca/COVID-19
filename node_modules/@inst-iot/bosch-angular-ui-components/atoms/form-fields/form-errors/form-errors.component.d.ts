import { ChangeDetectorRef, DoCheck, OnDestroy, OnInit, QueryList } from '@angular/core';
import { NgControl } from '@angular/forms';
import { FormValidationMessageDirective } from '../form-validation-message.directive';
export declare class FormErrorsComponent implements OnInit, OnDestroy, DoCheck {
    control: NgControl;
    private cd;
    messages: QueryList<FormValidationMessageDirective>;
    errors: any[];
    private sub;
    constructor(control: NgControl, cd: ChangeDetectorRef);
    ngOnInit(): void;
    ngOnDestroy(): void;
    ngDoCheck(): void;
    getErrors(): any[];
    getItemName(i: any, item: any): any;
}
