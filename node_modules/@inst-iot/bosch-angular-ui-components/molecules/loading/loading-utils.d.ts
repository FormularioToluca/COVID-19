import { Observable } from 'rxjs';
export declare class LoadingEntity<T> {
    loadingStateAfterFirstSuccess: boolean;
    runs: number;
    loading: boolean;
    error: any;
    result: T;
    private stopSubject;
    constructor(loadingStateAfterFirstSuccess?: boolean);
    /**
     * Tracks the status of an observable
     */
    run(observable: Observable<T>): Observable<T>;
    /**
     * This can complete and unsubscribe all observers
     */
    complete(): void;
    reset(): void;
}
