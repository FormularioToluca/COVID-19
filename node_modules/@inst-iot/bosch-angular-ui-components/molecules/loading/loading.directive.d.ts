import { ComponentFactoryResolver, DoCheck, TemplateRef, ViewContainerRef } from '@angular/core';
import { LoadingEntity } from './loading-utils';
export declare class LoadingDirective implements DoCheck {
    private templateRef;
    private viewContainer;
    private componentFactory;
    private loadingRef;
    private viewRef;
    private loadingEntity;
    private factory;
    rbLoadingSize: number;
    constructor(templateRef: TemplateRef<any>, viewContainer: ViewContainerRef, componentFactory: ComponentFactoryResolver);
    ngDoCheck(): void;
    rbLoading: boolean | LoadingEntity<any>;
    updateState(state: boolean | LoadingEntity<any>): void;
    updateView(state: boolean): void;
}
