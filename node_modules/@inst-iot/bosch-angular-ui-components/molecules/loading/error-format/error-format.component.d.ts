import { OnInit } from '@angular/core';
import { ErrorFormatter } from '../error-format.model';
export declare class ErrorFormatComponent implements OnInit {
    private formatters;
    error: any;
    opened: boolean;
    message: string;
    details: string;
    constructor(formatters: ErrorFormatter[]);
    ngOnInit(): void;
    identifyErrorData(error: any): string;
}
