import { AfterContentInit, ElementRef, OnDestroy, QueryList, Renderer2 } from '@angular/core';
import { Router, RouterLinkWithHref } from '@angular/router';
export declare class LoadingLinkDirective implements AfterContentInit, OnDestroy {
    private router;
    private element;
    private renderer;
    private subscription;
    private loading;
    className: string;
    linksWithHrefs: QueryList<RouterLinkWithHref>;
    constructor(router: Router, element: ElementRef, renderer: Renderer2);
    rbLoadingLink: string;
    ngAfterContentInit(): void;
    ngOnDestroy(): void;
    update(): void;
    private isRelevantUrl;
}
