import { PipeTransform } from '@angular/core';
import { ErrorFormatter } from './error-format.model';
/**
 * Formats an error to a string
 */
export declare class ErrorFormatPipe implements PipeTransform {
    formatters: ErrorFormatter[];
    constructor(formatters: ErrorFormatter[]);
    transform(error: any, args?: any): string;
}
