import { ActivatedRoute, ChildrenOutletContexts, Data, RouterOutlet } from '@angular/router';
import { ComponentFactoryResolver, ComponentRef, OnDestroy, OnInit } from '@angular/core';
/**
 * Mocks the Router Outlet
 */
export declare class TabPanelRouterOutletDirective extends RouterOutlet implements OnDestroy, OnInit {
    private _parentContexts;
    private __activated;
    private __activatedRoute;
    private _name;
    constructor(_parentContexts: ChildrenOutletContexts, name: string);
    ngOnInit(): void;
    readonly isActivated: boolean;
    readonly component: Object;
    readonly activatedRoute: ActivatedRoute;
    readonly activatedRouteData: Data;
    /**
     * Called when the `RouteReuseStrategy` instructs to detach the subtree
     */
    detach(): ComponentRef<any>;
    /**
     * Called when the `RouteReuseStrategy` instructs to re-attach a previously detached subtree
     */
    attach(ref: ComponentRef<any>, activatedRoute: ActivatedRoute): void;
    deactivate(): void;
    activateWith(activatedRoute: ActivatedRoute, resolver: ComponentFactoryResolver | null): void;
    onActivate(activatedRoute: ActivatedRoute): void;
}
