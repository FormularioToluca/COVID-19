import { OnInit, QueryList, TemplateRef } from '@angular/core';
import { BoardItemBodyDirective, BoardItemCountDirective, BoardItemIconDirective } from './board-item.directive';
export declare class BoardItemComponent implements OnInit {
    statusIcon: string | TemplateRef<any>;
    icon: BoardItemIconDirective;
    body: BoardItemBodyDirective;
    counts: QueryList<BoardItemCountDirective>;
    constructor();
    ngOnInit(): void;
    isTemplateStatus(): boolean;
}
