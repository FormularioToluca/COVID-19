import { OnDestroy, OnInit, QueryList } from '@angular/core';
import { AccordionTitleComponent } from './accordion-title/accordion-title.component';
import { AccordionBodyComponent } from './accordion-body/accordion-body.component';
import { AccordionService } from './accordion.service';
/**
 * <rb-accordion> is the container for
 * <rb-accordion-title> and <rb-accordion-body>
 */
export declare class AccordionComponent implements OnInit, OnDestroy {
    private accordion;
    /**
     * Whether only one can be opened.
     */
    openSingle: boolean;
    titles: QueryList<AccordionTitleComponent>;
    bodies: QueryList<AccordionBodyComponent>;
    private updateSub;
    constructor(accordion: AccordionService);
    ngOnInit(): void;
    ngOnDestroy(): void;
    updateState(title: AccordionTitleComponent): void;
}
