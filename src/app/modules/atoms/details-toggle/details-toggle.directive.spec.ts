import { DetailsToggleDirective } from './details-toggle.directive';

describe('DetailsToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new DetailsToggleDirective(null);
    expect(directive).toBeTruthy();
  });
});
