import { FormInputAutocompleteDirective } from './form-input-autocomplete.directive';

describe('FormInputAutocompleteDirective', () => {
  it('should create an instance', () => {
    const directive = new FormInputAutocompleteDirective(null, null, null, null, []);
    expect(directive).toBeTruthy();
  });
});
