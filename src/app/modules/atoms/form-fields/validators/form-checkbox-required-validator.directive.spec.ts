import { FormCheckboxRequiredValidatorDirective } from './form-checkbox-required-validator.directive';

describe('FormCheckboxRequiredValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new FormCheckboxRequiredValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
