import { FormFileValidatorDirective } from './form-file-validator.directive';

describe('FormFileValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new FormFileValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
