import { BoardItemIconDirective } from './board-item.directive';

describe('BoardItemDirective', () => {
  it('should create an instance', () => {
    const directive = new BoardItemIconDirective(null);
    expect(directive).toBeTruthy();
  });
});
