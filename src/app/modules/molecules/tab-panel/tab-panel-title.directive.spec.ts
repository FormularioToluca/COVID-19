import { TabPanelTitleDirective } from './tab-panel-title.directive';

describe('TabPanelTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new TabPanelTitleDirective(null);
    expect(directive).toBeTruthy();
  });
});
