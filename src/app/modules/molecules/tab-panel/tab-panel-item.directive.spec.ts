import { TabPanelItemDirective } from './tab-panel-item.directive';

describe('TabPanelItemDirective', () => {
  it('should create an instance', () => {
    const directive = new TabPanelItemDirective(null);
    expect(directive).toBeTruthy();
  });
});
