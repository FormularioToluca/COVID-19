import { SubBrandHeaderDirective } from './full-header.directive';

describe('FullHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new SubBrandHeaderDirective(null);
    expect(directive).toBeTruthy();
  });
});
