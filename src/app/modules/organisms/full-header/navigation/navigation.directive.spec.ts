import { MainNavItemsDirective } from './navigation.directive';

describe('NavigationDirective', () => {
  it('should create an instance', () => {
    const directive = new MainNavItemsDirective(null);
    expect(directive).toBeTruthy();
  });
});
